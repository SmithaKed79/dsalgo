#include <iostream>
#include <cmath>

using std::cout;
using std::endl;
//using std::string;

int main() {
    double x = 2.5;
    double y = 5.5;
    double z;
    //z = std::max(x,y);
    //z = std::min(x,y);
    //z = pow(2,3);
    //z = sqrt(9);
    //z = abs(-2);
    z = round(3.6);
    //z = ceil(3.14);
    //z = floor(3.14);
    cout<< z<<endl;
    return 0;
}